# ── Large binary embeddings & model weights (host on Kaggle instead) ─────────
*.npy
*.npz
*.pkl
*.keras
*.bin

# ── Pre-trained FastText (~6 GB) ─────────────────────────────────────────────
Pre-trained FastText Model/

# ── Personal notes / temp files ──────────────────────────────────────────────
*.docx
datafinal - Copy.csv
patch_v8.py

# ── Duplicate notebook ────────────────────────────────────────────────────────
siamese_bilstm_direct copy.ipynb

# ── Claude Code internal config ───────────────────────────────────────────────
.claude/

# ── Python / Jupyter cache ────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
